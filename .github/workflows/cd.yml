name: CD Pipeline

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  deploy:
    name: "Deploy on Tag"
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4

      - name: Copy env example
        run: cp .env.example .env

      - name: Build Docker image
        run: docker build -t passos-magicos-ml:${{ github.ref_name }} .

      # === OPÇÃO 1: Deploy no Heroku ===
      # - name: Login to Heroku
      #   env:
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      #   run: heroku container:login
      # - name: Push to Heroku
      #   run: |
      #     heroku container:push web -a ${{ secrets.HEROKU_APP_NAME }}
      #     heroku container:release web -a ${{ secrets.HEROKU_APP_NAME }}

      # === OPÇÃO 2: Push para Docker Hub ===
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      # - name: Push image
      #   run: |
      #     docker tag passos-magicos-ml:${{ github.ref_name }} ${{ secrets.DOCKER_USERNAME }}/passos-magicos-ml:${{ github.ref_name }}
      #     docker tag passos-magicos-ml:${{ github.ref_name }} ${{ secrets.DOCKER_USERNAME }}/passos-magicos-ml:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/passos-magicos-ml:${{ github.ref_name }}
      #     docker push ${{ secrets.DOCKER_USERNAME }}/passos-magicos-ml:latest

      # === OPÇÃO 3: Deploy no Google Cloud Run ===
      # - name: Auth GCP
      #   uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}
      # - name: Deploy to Cloud Run
      #   uses: google-github-actions/deploy-cloudrun@v2
      #   with:
      #     service: passos-magicos-ml
      #     image: gcr.io/${{ secrets.GCP_PROJECT }}/passos-magicos-ml:${{ github.ref_name }}
      #     region: us-central1

      # === OPÇÃO 4: Deploy na AWS (ECR + ECS) ===
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}
      # - name: Login to Amazon ECR
      #   uses: aws-actions/amazon-ecr-login@v2
      # - name: Push to ECR and deploy ECS
      #   run: |
      #     docker tag passos-magicos-ml:${{ github.ref_name }} ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/passos-magicos-ml:${{ github.ref_name }}
      #     docker push ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/passos-magicos-ml:${{ github.ref_name }}

      - name: Notify deploy success
        run: echo "✅ Deploy da versão ${{ github.ref_name }} realizado com sucesso!"
